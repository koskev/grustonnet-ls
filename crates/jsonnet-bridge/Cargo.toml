[package]
name = "jsonnet-bridge"
version = "0.1.0"
edition = "2024"

[dependencies]
rust2go = { version = "0.4.0" }
schemars = "1.0.2"
serde = { version = "1.0.219", features = ["derive"] }
smart-default = "0.7.1"

[build-dependencies]
rust2go = { version = "0.4.0", features = ["build"] }
