name: Validate MR

on:
  pull_request:

jobs:
  conform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
      - name: Install conform
        run: go install github.com/siderolabs/conform/cmd/conform@v0.1.0-alpha.30
      - name: Skip the merge commit github creates
        run: git checkout HEAD^2
      - name: Run conform
        run: conform enforce --base-branch remotes/origin/main~1


