# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/mr.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  conform:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.sha }}
    - uses: actions/setup-go@v6
      with:
        go-version: '1.25'
    - name: Install conform
      run: go install github.com/siderolabs/conform/cmd/conform@v0.1.0-alpha.30
    - name: Run conform
      run: conform enforce --base-branch remotes/origin/main
'on':
  pull_request: {}
