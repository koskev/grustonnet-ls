# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/docs.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
    - name: checkout full
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - name: Most important Action!
      uses: wimpysworld/nothing-but-nix@687c797a730352432950c707ab493fcc951818d7
      with:
        hatchet-protocol: rampage
    - name: Install nix
      uses: cachix/install-nix-action@v31
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - run: cd docs && nix develop ..#docs --command make build && cd ..
    - name: Upload artifacts for pages
      uses: actions/upload-pages-artifact@v4
      with:
        path: docs/book
  pages:
    environment:
      name: github-pages
      url: ${{steps.deployment.outputs.page_url}}
    needs:
    - docs
    permissions:
      id-token: write
      pages: write
    runs-on: ubuntu-latest
    steps:
    - id: deployment
      name: Deploy to GitHub Pages
      uses: actions/deploy-pages@v4
'on':
  push:
    branches:
    - main
